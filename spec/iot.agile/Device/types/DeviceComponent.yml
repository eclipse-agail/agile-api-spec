DeviceComponent:
  description: A device component from which get a data stream, like a sensor
  type: Object
  fields:
    id:
      type: String
    unit:
      type: String
      required: false
    properties:
      type: KeyValue
      required: false
